import phone from './normalizes/normalizePhone'
import numbersOnly from './normalizes/normalizeOnlyNumbers'

export const normalizePhone = () => phone
export const normalizeNumbersOnly = () => numbersOnly
